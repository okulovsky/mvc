<h2>Hello, MVC!</h2>

Today is @DateTime.Now.ToString("dd-MM-yyyy")


<table>
@for (int i = 1; i <= 9; i++)
{
    <tr>
    @for (int j = 1; j <= 9; j++)
    {
        <td>@((i * j).ToString())</td>
    }
    </tr>
} 
</table>